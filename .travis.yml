language: python
python:
  - 2.7
  - 3.5

addons:
  apt:
    packages:
      - colordiff
install:
  - pip install --requirement requirements.txt
  - pip install ansible
  - pip install enum34
  - pip install pathlib
  - pip install typing
script:
  - vint .vimrc*
  - yamllint .
  - ./test.sh

dist: focal

addons:
  apt:
    packages:
      - colordiff
install:
  - pip install --requirement requirements.txt
  - pip install ansible
  - pip install enum34
  - pip install pathlib
  - pip install typing
script:
  - pre-commit run --all-files
  - ./run_tests.sh

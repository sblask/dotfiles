- hosts: all

  tasks:

    - name: Install homebrew packages
      homebrew:
        name: "{{ item }}"
      loop:
        - colordiff
        - coreutils
        - docker-credential-helper-ecr
        - entr
        - fd
        - gnu-sed
        - gnu-tar
        - gpg
        - grep
        - htop
        - jo
        - jq
        - moreutils
        - neovim
        - oath-toolkit
        - rename
        - ripgrep
        - shellcheck
        - terraform-docs
        - tmux
        - tree

    - name: Install homebrew casks
      homebrew_cask:
        name: "{{ item }}"
      loop:
        - alt-tab
        - aws-vault
        - firefox
        - inkscape
        - postman
        - rectangle

# vim: ft=ansible.yaml

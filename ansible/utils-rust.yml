- name: Play to install Cargo packages
  hosts: all

  vars:
    packages:
      selene: 0.28.0  # renovate: Kampfkarren/selene
      stylua: 2.0.2  # renovate: JohnnyMorganz/StyLua

  tasks:

    - name: Install Cargo packages
      community.general.cargo:
        name: "{{ item.key }}"
        version: "{{ item.value }}"
      loop: "{{ packages | dict2items }}"

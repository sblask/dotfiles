#!/bin/bash

set -o errexit -o nounset -o pipefail -o xtrace

PID="$1"
shift

COMMAND="$*"

CHILD_PID=$(get-child-pid-with-name "${PID}" /bin/zsh /usr/bin/zsh)
CHILD_CWD=$(pid-to-cwd "${CHILD_PID}")

$COMMAND "${CHILD_CWD}"

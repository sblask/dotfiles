(if
    (and
        (not (is (window_name) "Find"))
        (not (contains (window_name) "CSSH"))
        (is (window_class) "Xfce4-terminal")
        (is (application_name) "Xfce Terminal")
    )
    (maximize)
)

